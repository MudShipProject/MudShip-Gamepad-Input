{
    "name": "com.yamamotoryo0212.gamepad-input",
    "displayName": "MudShip Gamepad Input",
    "author": "mudship",
    "version": "1.1.0",
    "unity": "6000.0",
    "description": "Many Xbox controllers (soft cap 32) with full background capture via Windows GameInput API. Static API: MS_Gamepad.GetState(index, GamepadInput). Windows x64 only (native plugin).",
    "dependencies": {
    }
}
